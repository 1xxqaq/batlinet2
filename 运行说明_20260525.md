# BatLiNet-main2 运行说明（2026-05-25）

本文档用于说明当前 `BatLiNet-main2` 项目的推荐运行方式，尤其是：

- 服务器上的目录组织
- 数据放置位置
- 原始 BatLiNet checkpoint 放置位置
- 固定测试参考协议的生成与使用
- 原始 BatLiNet 与 `supervised_weighted` 的公平对比流程


## 1. 适用前提

当前代码默认：

1. 实验主要在服务器上运行，不在本地完整跑训练/测试。
2. Git 仓库只保存代码、配置和脚本，不保存：
   - 数据集
   - checkpoint
   - `workspaces/`
   - `analysis/`
   - 固定测试协议文件
3. 服务器上会补齐：
   - `data/processed/*`
   - 原始 BatLiNet checkpoint
   - 后续 `supervised_weighted` checkpoint


## 2. 推荐服务器目录结构

推荐在服务器上组织成下面这样：

```text
BatLiNet-main2/
    configs/
    scripts/
    src/
    data/
        processed/
            CALCE/
            RWTH/
            UL_PUR/
            SNL/
            MATR/
            HUST/
            HNEI/
    artifacts/
        fixed_test_support_indices/
            mix_20/
                seed_0.pt
                seed_1.pt
                ...
                seed_7.pt
    checkpoints/
        batlinet_original_mix_20_0424/
            20260424032049_seed_0_epoch_1000.ckpt
            20260424032721_seed_1_epoch_1000.ckpt
            ...
            20260424040605_seed_7_epoch_1000.ckpt
    workspaces/
```

说明：

- `data/processed/*` 是正式运行所需数据
- `artifacts/fixed_test_support_indices/mix_20/` 存放固定测试参考协议
- `checkpoints/batlinet_original_mix_20_0424/` 存放原始 BatLiNet 旧 checkpoint
- `workspaces/` 存放重新推理或后续训练得到的新结果


## 3. 固定测试参考协议是什么

固定测试参考协议用于保证：

> 原始 BatLiNet 和 `supervised_weighted` 在测试时，对同一个目标电池使用完全相同的一组参考电池。

每个协议文件对应一个 seed，例如：

```text
artifacts/fixed_test_support_indices/mix_20/seed_0.pt
```

每份协议文件记录：

- 当前 seed
- 测试样本数
- `test_support_size`
- 一个形状为 `[num_test_samples, 32]` 的索引张量

含义是：

- 每一行对应一个测试目标电池
- 这一行中的 32 个整数表示它对应使用的 32 个训练参考电池索引


## 4. 为什么要生成 8 份协议

因为当前实验以 8 个 seed 作为 8 次独立重复。

因此推荐：

- `seed_0` 对应一份协议
- `seed_1` 对应一份协议
- ...
- `seed_7` 对应一份协议

同一个 seed 下：

- 原始 BatLiNet
- `supervised_weighted`

共用同一份协议。

这样可以保证：

1. 每个 seed 内部比较公平
2. 不同 seed 之间仍然保留独立重复实验的意义


## 5. 生成固定测试参考协议

脚本：

```text
scripts/generate_fixed_test_support_indices.py
```

示例命令：

```bash
PYTHONPATH=. python scripts/generate_fixed_test_support_indices.py \
  configs/ablation/diff_branch/batlinet/mix_20.yaml \
  --seed 0 \
  --test-support-size 32 \
  --output-path artifacts/fixed_test_support_indices/mix_20/seed_0.pt
```

需要为 `seed=0..7` 各生成一份。

如果想批量生成，可以按这个形式循环：

```bash
for seed in 0 1 2 3 4 5 6 7; do
  PYTHONPATH=. python scripts/generate_fixed_test_support_indices.py \
    configs/ablation/diff_branch/batlinet/mix_20.yaml \
    --seed ${seed} \
    --test-support-size 32 \
    --output-path artifacts/fixed_test_support_indices/mix_20/seed_${seed}.pt
done
```


## 6. 原始 BatLiNet checkpoint 放哪里

推荐放在：

```text
checkpoints/batlinet_original_mix_20_0424/
```

原因：

1. 语义清晰
2. 与项目代码分离
3. 不污染 `workspaces/`
4. 后续也可以为 `supervised_weighted` 单独放 checkpoint 目录

你之前本地复现得到的原始 BatLiNet checkpoint 原路径是：

```text
D:\Documents\研一\论文阅读\复现代码\Batlinet\BatLiNet-main\workspaces\mix_20_0424
```

上传到服务器时，建议把其中的 `*.ckpt` 单独整理到上面的 `checkpoints/` 目录下。


## 7. 原始 BatLiNet 如何用固定协议重新推理

原则上需要：

1. 加载原始 BatLiNet 配置
2. 指定 `checkpoint`
3. 指定固定测试参考协议路径
4. 关闭训练，仅做评估

推荐思路是：

- 对每个 seed：
  - 使用对应 seed 的 checkpoint
  - 使用对应 seed 的固定测试协议
  - 重新生成新的 `predictions_seed_*.pkl`

这样得到的结果才适合与 `supervised_weighted` 做公平对比。

注意：

> 不能直接拿旧的 `predictions_seed_*.pkl` 做这次公平对比。  
> 因为旧 `pkl` 对应的是旧的随机参考电池采样。


## 8. `supervised_weighted` 如何用固定协议重新推理

流程与原始 BatLiNet 相同：

1. 使用 `configs/ablation/diff_branch/batlinet_supervised_weighted/mix_20.yaml`
2. 加载对应 checkpoint
3. 指定与原始 BatLiNet 完全相同的固定测试协议路径
4. 重新推理

这样比较的才是：

> 同一测试目标电池 + 同一参考电池集合下，原始聚合与有监督加权聚合的差异。


## 9. 当前 `.gitignore` 约定

当前仓库已经默认忽略：

- `/data`
- `/workspaces`
- `/analysis`
- `/artifacts`
- `*.pkl`
- `*.ckpt`
- `log.*`

因此：

- 代码、配置、脚本可以提交
- 数据、checkpoint、结果文件、协议文件不会进 Git


## 10. 推荐实验流程

### 10.0 结果目录规范

后续所有结果，统一建议放在：

```text
workspaces/fair_eval/
```

按“模型版本 / 任务名 / 协议版本”分层，例如：

```text
workspaces/fair_eval/
    batlinet_original/
        mix_20/
            protocol_v1/
                config_*.yaml
                log.*
                predictions_seed_*.pkl
    batlinet_supervised_weighted/
        mix_20/
            protocol_v1/
                config_*.yaml
                log.*
                predictions_seed_*.pkl
```

其中：

- `batlinet_original/`：原始 BatLiNet
- `batlinet_supervised_weighted/`：有监督参考电池可信度加权
- `mix_20/`：任务名
- `protocol_v1/`：固定测试协议版本

这样做的目的是：

1. 后续增加新方法时不混结果
2. 协议一旦变化，可以直接用 `protocol_v2/`、`protocol_v3/`
3. 后续分析和画图时，能一眼看出结果来自哪个方法、哪个任务、哪份协议

### 10.1 第一阶段：建立公平测试协议

1. 服务器放好 `data/processed/*`
2. 生成 `seed_0..7` 共 8 份固定测试协议
3. 保存到：

```text
artifacts/fixed_test_support_indices/mix_20/
```

建议当前第一版协议目录命名为：

```text
artifacts/fixed_test_support_indices/mix_20/protocol_v1/
```

也就是：

```text
artifacts/fixed_test_support_indices/mix_20/protocol_v1/seed_0.pt
...
artifacts/fixed_test_support_indices/mix_20/protocol_v1/seed_7.pt
```

### 10.2 第二阶段：原始 BatLiNet 重新推理

1. 把旧 checkpoint 放到：

```text
checkpoints/batlinet_original_mix_20_0424/
```

2. 对每个 seed 用固定协议重新跑评估
3. 得到新的公平对比结果

推荐输出目录：

```text
workspaces/fair_eval/batlinet_original/mix_20/protocol_v1
```

### 10.3 第三阶段：`supervised_weighted` 重新推理

1. 准备 `supervised_weighted` checkpoint
2. 对每个 seed 使用同一份固定协议重新推理
3. 与原始 BatLiNet 做公平对比

推荐输出目录：

```text
workspaces/fair_eval/batlinet_supervised_weighted/mix_20/protocol_v1
```

## 10.4 第一次实际运行建议

如果你现在先只想把“原始 BatLiNet 的公平评估”跑起来，建议按这个顺序执行。

### 步骤 1：生成固定测试协议

```bash
mkdir -p artifacts/fixed_test_support_indices/mix_20/protocol_v1

for seed in 0 1 2 3 4 5 6 7; do
  PYTHONPATH=. python scripts/generate_fixed_test_support_indices.py \
    configs/ablation/diff_branch/batlinet/mix_20.yaml \
    --seed ${seed} \
    --test-support-size 32 \
    --output-path artifacts/fixed_test_support_indices/mix_20/protocol_v1/seed_${seed}.pt
done
```

### 步骤 2：用旧 checkpoint 重新评估原始 BatLiNet

```bash
mkdir -p workspaces/fair_eval/batlinet_original/mix_20/protocol_v1

./scripts/evaluate_with_fixed_protocol.sh \
  configs/ablation/diff_branch/batlinet/mix_20.yaml \
  checkpoints/batlinet_original_mix_20_0424 \
  artifacts/fixed_test_support_indices/mix_20/protocol_v1 \
  workspaces/fair_eval/batlinet_original/mix_20/protocol_v1 \
  8
```

### 步骤 3：检查输出

跑完以后，每个 seed 应该至少有：

- `log.0` 到 `log.7`
- `config_*.yaml`
- `predictions_seed_*.pkl`

并且新的 `predictions_seed_*.pkl` 里会额外保存：

- `metadata.train_cells`
- `metadata.test_cells`
- `evaluation_protocol.fixed_test_support_index_path`
- `diagnostics.support_index`
- `diagnostics.support_weight`
- `diagnostics.y_ori`
- `diagnostics.y_sup`
- `diagnostics.y_sup_agg`

其中：

- 对原始 BatLiNet，`support_weight` 会是 `None`
- 对 `supervised_weighted`，`support_weight` 会有实际权重


## 11. GitHub 仓库信息

当前 GitHub 仓库：

```text
https://github.com/1xxqaq/batlinet2
```

推荐约定：

- 默认主分支：`main`
- 代码只推送：
  - `src/`
  - `scripts/`
  - `configs/`
  - 说明文档
- 不推送：
  - `data/`
  - `workspaces/`
  - `analysis/`
  - `artifacts/`
  - `*.ckpt`
  - `*.pkl`

本地首次关联远程的典型命令：

```bash
git remote add origin https://github.com/1xxqaq/batlinet2.git
git branch -M main
git push -u origin main
```

服务器更新代码的典型命令：

```bash
git pull
```

如果服务器首次拉取：

```bash
git clone https://github.com/1xxqaq/batlinet2.git
cd batlinet2
```

## 12. 当前新增脚本

### 12.1 生成固定测试参考协议

```text
scripts/generate_fixed_test_support_indices.py
```

作用：

- 为每个 seed 生成一份固定测试参考协议

### 12.2 使用固定协议批量评估旧 checkpoint

```text
scripts/evaluate_with_fixed_protocol.sh
```

作用：

- 给定：
  - 配置文件
  - checkpoint 目录
  - 固定协议目录
  - 输出 workspace
  - seed 数量
- 自动按 seed 批量重新推理

示例：

```bash
./scripts/evaluate_with_fixed_protocol.sh \
  configs/ablation/diff_branch/batlinet/mix_20.yaml \
  checkpoints/batlinet_original_mix_20_0424 \
  artifacts/fixed_test_support_indices/mix_20 \
  workspaces/fair_eval/batlinet_original_mix_20 \
  8
```
